import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 5,
    borderRadius: 10,
    marginEnd: 24,
  },
  selectedContainer: {
    backgroundColor: "#3EBDAC",
  },
});

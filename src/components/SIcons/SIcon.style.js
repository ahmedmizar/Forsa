
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  rotate: { transform: [{ scaleX: -1 }], alignItems: 'flex-end' },
  backdrop: { backgroundColor: 'transparent' },

});
